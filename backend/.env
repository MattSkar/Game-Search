# This is an example environment file.
# For local development, copy this file to a new file named '.env'.
# For production, set these variables in your hosting service's dashboard.

# The connection URL for your Redis instance.
# This default value works for a standard local Redis installation.
# Your hosting provider (e.g., Render) will provide a different URL for production.
REDIS_URL=redis://localhost:6379

# A comma-separated list of URLs that are allowed to make requests to this API.
# For local development, the React server runs on port 5173.
# In production, you MUST change this to your live frontend URL (e.g., "https://your-app-name.netlify.app") for security.
CORS_ORIGIN=http://localhost:5173
